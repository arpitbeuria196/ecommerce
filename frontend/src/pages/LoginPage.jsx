const LoginPage = () => {
  return (
    <div
      style={{
        height: "100vh",
        color: "white",
        fontSize: "2rem",
        fontWeight: "bold",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      LoginPage
    </div>
  );
};

export default LoginPage;
